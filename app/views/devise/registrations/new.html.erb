<div class="signup-form">
  <%= simple_form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= f.error_notification %>

    <div class="form-inputs">
      <%= f.input :name, required: true, label: false, placeholder: "名称" %>
      <%= f.input :email, required: true, autofocus: true, label: false, placeholder: "邮箱" %>
      <%= f.input :password, required: true, label: false, placeholder: "密码" %>
      <%= f.input :password_confirmation, required: true, label: false, placeholder: "确认密码" %>
      <div class="form-actions">
        <%= f.button :submit, "注册", class: "btn-success" %>
      </div>
    </div>
  <% end %>
</div>
